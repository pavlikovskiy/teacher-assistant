// @ts-nocheck

describe('temperature model test', () => {
  test('Kelvin from Celsius', () => {
    expect(1 + 2).toBe(3);
  });
  test('Rankine from Celsius', () => {
    expect(1 + 2).toBe(3);
  });
  test('Fahrenheit from Celsius', () => {
    expect(1 + 2).toBe(3);
  });

  test('Celsius from Fahrenheit', () => {
    expect(1 + 2).toBe(3);
  });
})

describe('volume model test', () => {
  test('Liter from Gallon', () => {
    expect(1 + 2).toBe(3);
  });
  test('Gallon from Liter', () => {
    expect(1 + 2).toBe(3);
  });
})
